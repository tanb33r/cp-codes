#include <bits/stdc++.h>
#define  f(i,n)             for(int i=0; i<(n); i++)
#define  fast               ios::sync_with_stdio(0),cin.tie(0);
using namespace std;

int main() {
    fast;
    int TEST=1;
    cin>>TEST;
    while(TEST--) {
        int a,b,c,d ;
        cin>>a>>b>>c>>d;
        cout<<1<<" "<<1<<" "<<a<<" "<<b<<"\n";
    }
}
